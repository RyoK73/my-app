import { PostListByTag } from "@/components/common/PostList";

const App = async () => {
    return (
        <main className="">
            <PostListByTag />
        </main>
    );
};

export default App;
